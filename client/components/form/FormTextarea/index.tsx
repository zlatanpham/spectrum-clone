import createFormElement from '../createFormElement';
import Textarea, { TextareaProps } from '../../Textarea';

const FormTextarea = createFormElement<TextareaProps>(Textarea);

export default FormTextarea;
