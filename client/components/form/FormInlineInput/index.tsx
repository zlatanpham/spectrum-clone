import createFormElement from '../createFormElement';
import InlineInput, { InlineInputProps } from '../../InlineInput';

const FormInlineInput = createFormElement<InlineInputProps>(InlineInput);

export default FormInlineInput;
