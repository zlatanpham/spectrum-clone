{
  "name": "spectrum-clone",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "lerna exec -- yarn start",
    "start:client": "lerna exec --scope=@spectrum/client -- yarn start",
    "start:admin": "lerna exec --scope=@spectrum/admin -- yarn start",
    "start:api": "lerna exec --scope=@spectrum/api -- yarn start"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "./client",
    "./admin",
    "./api"
  ]
}
